1. 解决线程池退出段错误的问题
如果执行时间很长, 那么退出的时候就会出现段错误
线程多了之后也可能会发生错误
2. 解决线程池的测试为类的函数或者lambda函数的时候
线程不能交替运行, 有一个线程会独占很长时间的才会切换线程
2. 为TinySTL添加测试代码
3. 为TinySTL写CMakeLists.txt, 将编译后的文件放到bin目录下的相应的位置
4. 更新简历
5. 如果可以, 那么添加posix标准的线程池
6. 整理thread.org